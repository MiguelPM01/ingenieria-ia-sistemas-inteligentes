/* Crear un objeto que represente a un usuario
Luego:
- imprima el nombre.
- imprima la edad.
- imprima si está activo. */

const usuario = {
    nombre: 'Miguel',
    edad: 24,
    activo: true
};

console.log(usuario.nombre);
console.log(usuario.edad);
console.log(usuario.activo);

