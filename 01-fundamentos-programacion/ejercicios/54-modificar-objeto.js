/*Crear un objeto usuario como el anterior
Cambiar la edad a otro valor.
Cambiar activo a false.
-imprimir el objeto completo. */

let usuario = {
    nombre: 'Miguel',
    edad: 24,
    activo: true
};

usuario.edad = 30;
usuario.activo = false;

console.log(usuario);